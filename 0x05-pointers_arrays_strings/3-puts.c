#include "main.h"
#include <stdio.h>
/**
 * _put - function that prints a
 * string, followed by a new line,
 * to stdout.
 *
 * @str: string to be printed to stdout
 *
 * Return: nothing
 */

void _puts(char *str)
{
	puts(str);
}
